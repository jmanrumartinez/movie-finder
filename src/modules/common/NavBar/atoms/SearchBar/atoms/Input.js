import styled from 'styled-components';

export default styled.input`
    padding-left: 5px; 
    background: inherit;
    border: none; 
    width: 100%; 
    color: #ffffff;
    font-size: 0.95em; 
    &:focus {
        outline: none
    }
`;
